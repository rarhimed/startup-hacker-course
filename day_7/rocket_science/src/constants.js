/**
 * Границы зоны полёта кораблей
 * Определяет отступы от краёв экрана, чтобы корабли не залетали под UI-элементы
 */
export const FLIGHT_BOUNDS = {
  left: 200,      // Отступ от левого края (ширина меню кораблей)
  right: 50,      // Отступ от правого края
  top: 100,       // Отступ сверху (статистика)
  bottom: 150     // Отступ снизу
};
